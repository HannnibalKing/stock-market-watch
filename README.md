# Stock Market Watch Dashboard

Watchlist-focused quote API baseline.

```bash
npm test
npm start
```

Endpoint: `GET /api/watchlist`. Add a UI, alerts, and provider adapter over this normalized watchlist response when product requirements are defined.
