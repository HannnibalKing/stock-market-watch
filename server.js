const http=require('node:http');const watchlist=[{symbol:'AAPL',price:212.4,change:0.8},{symbol:'TSLA',price:341.1,change:-1.7},{symbol:'AMZN',price:231.6,change:1.4}];function json(res,status,value){res.writeHead(status,{'Content-Type':'application/json'});res.end(JSON.stringify(value));}const server=http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');if(url.pathname==='/health')return json(res,200,{status:'ok'});if(url.pathname==='/api/watchlist')return json(res,200,{watchlist,source:'local-fixture',updatedAt:new Date().toISOString()});return json(res,404,{error:'not found'});});module.exports={watchlist};if(require.main===module)server.listen(Number(process.env.PORT||3000),()=>console.log('Stock market watch dashboard on http://localhost:'+(process.env.PORT||3000)));
